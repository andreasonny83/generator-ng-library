language: node_js

cache:
  directories:
    - node_modules

notifications:
  email: false

node_js:
  - '7'

before_script:
  - npm prune

script:
  - scripts/build.sh
  # - scripts/test.sh

after_success:
  # - npm run coveralls
  # - npm run semantic-release
  - scripts/deploy.sh

branches:
  except:
    - /^v\d+\.\d+\.\d+$/
